"use client";

import { useSiteEffects } from "./hooks";

/** Sayt bo'ylab umumiy animatsiyalarni yoqadi (reveal, counter, magnetic, header) */
export function SiteEffects() {
  useSiteEffects();
  return null;
}
